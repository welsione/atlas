{
  "name": "aibase",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "workspaces": [
    "packages/types",
    "packages/core",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build -w @atlas/types && npm run build -w @atlas/core",
    "test": "npm run test -w @atlas/core",
    "dev": "npm run start:dev -w @atlas/core",
    "build:web": "npm run build -w @atlas/web",
    "sync:static": "npm run build -w @atlas/web && rm -rf packages/core/static && cp -R packages/web/dist packages/core/static"
  },
  "engines": {
    "node": ">=22"
  }
}
