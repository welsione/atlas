# 安全策略

Atlas 是一个多租户 AI 服务基础平台，安全是核心关注点。本文档说明如何报告安全漏洞，以及平台的安全设计边界。

## 支持版本

| 版本 | 支持状态 |
|------|----------|
| 0.2.x | ✅ 当前支持 |
| < 0.2.0 | ❌ 不再支持 |

## 报告漏洞

**请勿在公开 issue 中披露安全漏洞。**

如发现安全漏洞，请通过以下方式**私下**报告：

1. 在 GitHub 使用「Report a vulnerability」（Security 标签页的 Private vulnerability reporting）。
2. 或联系项目维护者（见仓库主页）。

请提供以下信息以加速处理：

- 受影响版本与部署环境
- 复现步骤（最小化 PoC）
- 潜在影响评估
- 建议的修复方向（如有）

## 处理流程

1. 收到报告后，维护者将在 **3 个工作日内**确认收到。
2. 评估影响范围与严重性，并与报告者保持沟通。
3. 修复将先在私有分支完成，随安全版本发布。
4. 发布后，在 CHANGELOG 与安全公告中披露（遵循 90 天协调披露原则，除非双方同意更早）。

## 安全设计边界（开发者须知）

部署 Atlas 时请理解以下安全前提：

- **`ATLAS_ENC_KEY` 是机密**：数据集信封加密（SECRET 级）与插件密钥派生（`env.crypto()`）都依赖它。生产环境（启用管理认证）若未显式配置，平台会**拒绝启动**。
- **管理认证**：`ATLAS_ADMIN_PASSWORD` / `ATLAS_ADMIN_KEY` 至少配置其一；两者都未配置时管理接口对本地开放（仅限开发）。
- **`ATLAS_TRUST_PROXY`**：默认不信任 `X-Forwarded-For`（防伪造 IP 绕过黑名单/限流/审计），仅在可信反向代理后开启。
- **数据面鉴权**：`/api/v1` 下应用令牌（Bearer）访问；SECRET 级数据集需白名单授权 + 逐次审计 + 信封加密。
- **插件是信任边界**：插件可读写其作用域内数据，请仅安装可信来源的插件。

平台内置的防御机制（路径防穿越、Content-Disposition 注入防护、IP 限流、审计日志、SVG 消毒）见各规范文档的「安全红线」章节。
